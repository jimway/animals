<%= form_with(model: animal) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :search_name, style: "display: block" %>
    <%= form.text_area :search_name %>
  </div>

  <div>
    <%= form.label :image_name, style: "display: block" %>
    <%= form.text_area :image_name %>
  </div>

  <div>
    <%= form.label :habitat, style: "display: block" %>
    <%= form.text_field :habitat %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
